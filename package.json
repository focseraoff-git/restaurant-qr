{
    "name": "restaurant-qr-system-root",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "start": "npm start --prefix backend",
        "build": "npm install && npm run build --prefix frontend",
        "install-all": "npm install"
    }
}